<h1>Pokemon Dictionary</h1>
<div class="columns">

  <div class="column">
    <ul>
      <% @pokemons.each do |p| %>
        <li>
          <%= render partial: 'pokemons/pokemon', locals: {pokemon: p} %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="column">
    <ul>
      <% @regions.each do |r| %>
        <li>
          <%= link_to r.name, r %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="column">
    <ul>
      <% @types.each do |t| %>
        <li>
          <%= link_to t.name, t %>
        </li>
      <% end %>
    </ul>
  </div>

  <div class="column">
    <ul>
      <% @abilities.each do |a| %>
        <li>
          <%= link_to a.name, a %>
        </li>
      <% end %>
    </ul>
  </div>

</div>
